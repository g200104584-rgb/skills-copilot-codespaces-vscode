# دليل روبوت Rux - بناء روبوت من الصفر

## نظرة عامة
تم بناء هذا الروبوت (Rux) من الصفر بلغة Python. إنه روبوت محادثة ذكي يمكنه التعلم والتفاعل معك.

## المميزات الرئيسية

### 1. المحادثة الطبيعية
الروبوت يمكنه:
- الترحيب بك
- الرد على أسئلتك
- تذكر المحادثات
- الوداع بطريقة ودية

### 2. نظام التعلم
- علّم الروبوت ردود جديدة
- أضف معرفة جديدة في فئات مختلفة
- الروبوت يتذكر ما تعلمه

### 3. الإحصائيات
- تتبع عدد المحادثات
- معرفة عدد الردود المتاحة
- مشاهدة سجل المحادثات

### 4. نظام المزاج
- الروبوت لديه مزاج يمكن تغييره
- يؤثر المزاج على أسلوب الردود

### 5. حفظ واستعادة المعرفة
- احفظ معرفة الروبوت في ملف
- استعد المعرفة في جلسة لاحقة

## كيفية الاستخدام

### التشغيل السريع

#### 1. تشغيل الروبوت التفاعلي
```bash
python robot.py
```

#### 2. تشغيل العروض التوضيحية
```bash
python demo.py
```

#### 3. تشغيل الاختبارات
```bash
python test_robot.py
```

### مثال على الاستخدام

```python
from robot import Robot

# إنشاء روبوت
robot = Robot(name="Rux")

# التحية
print(robot.greet())  # مرحباً! أنا Rux

# محادثة
print(robot.respond("كيف حالك؟"))  # أنا بخير! شكراً للسؤال

# تعليم شيء جديد
robot.learn("نكتة", "لماذا عبر الروبوت الطريق؟ للوصول إلى الجانب الآخر!")

# حفظ المعرفة
robot.save_knowledge("my_robot.json")
```

## الأوامر التفاعلية

عند تشغيل `python robot.py`:
- اكتب رسالتك للتحدث مع الروبوت
- اكتب `quit` أو `exit` لإنهاء المحادثة
- اكتب `stats` لعرض إحصائيات الروبوت
- اكتب `learn <فئة> <رد>` لتعليم الروبوت

## أمثلة المحادثات

### مثال 1: محادثة بسيطة
```
أنت: مرحباً!
Rux: أهلاً! Rux في خدمتك!

أنت: كيف حالك؟
Rux: أشعر بالسعادة! شكراً للسؤال!

أنت: شكراً!
Rux: على الرحب والسعة!
```

### مثال 2: تعليم الروبوت
```
أنت: learn greeting مرحباً بك!
Rux: شكراً! لقد تعلمت رداً جديداً للـ greeting!
```

## الملفات المهمة

- `robot.py` - تنفيذ الروبوت الرئيسي
- `demo.py` - مجموعة العروض التوضيحية (7 عروض)
- `test_robot.py` - اختبارات الروبوت
- `robot_config.json` - ملف التكوين
- `ROBOT_README.md` - دليل مفصل بالإنجليزية

## الميزات المتقدمة

### حفظ واستعادة المعرفة
```python
# حفظ
robot.save_knowledge("knowledge.json")

# استعادة
robot.load_knowledge("knowledge.json")
```

### تغيير المزاج
```python
robot.set_mood("متحمس")
robot.set_mood("سعيد")
robot.set_mood("مفكر")
```

### الحصول على الإحصائيات
```python
stats = robot.get_stats()
print(stats)
```

### سجل المحادثات
```python
history = robot.get_conversation_history()
for entry in history:
    print(f"{entry['sender']}: {entry['message']}")
```

## التطويرات المستقبلية

يمكن تحسين الروبوت بإضافة:
- معالجة اللغة الطبيعية المتقدمة
- الذكاء الاصطناعي للتعلم الآلي
- دعم اللغة العربية الكامل
- واجهة صوتية
- واجهة ويب
- قاعدة بيانات

## المتطلبات

- Python 3.7 أو أحدث
- لا توجد مكتبات خارجية مطلوبة

## الدعم والمساعدة

للحصول على دليل مفصل بالإنجليزية، راجع `ROBOT_README.md`

## الخلاصة

الآن لديك روبوت محادثة كامل مبني من الصفر! يمكنك:
1. التحدث معه
2. تعليمه أشياء جديدة
3. حفظ واستعادة معرفته
4. تتبع الإحصائيات
5. تخصيص سلوكه

استمتع باستخدام روبوت Rux! 🤖
